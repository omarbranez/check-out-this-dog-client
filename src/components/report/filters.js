import React, { useState, useEffect } from 'react'
import DataGrid from '@mui/material/DataGrid'

const Filters = (props) => {
    return(
        <form>
            <label>
                Sort: 
                <input name="checkbox"
            </label>
        </form>
    )
}